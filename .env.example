# Required in production. Change this to a private school access code.
SCHOOL_ACCESS_CODE=change-me

# Render/Railway usually provide PORT automatically.
PORT=8765

# Local default is ./data. For Render persistent disk use /var/data.
APP_DATA_DIR=data

# Set to true only when the app is served over HTTPS.
COOKIE_SECURE=false
